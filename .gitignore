build/
.DS_Store
xcuserdata/
PlayerPROCore.opensdf
PlayerPROCore.sdf
Debug
Release
*.vcxproj.user
ipch
*vers.plist
PlayerPROCore.suo
project.xcworkspace/
*.xccheckout

**~

CMakeCache.txt
Makefile
**/CMakeFiles
**/cmake_install.cmake
MADDriver.source/libPlayerPROCore*
lib*.so

Examples/PPPlay/PPPlay
Examples/PPPlay/PPPlay.exe
*.xcscmblueprint
timeline.xctimeline

# Spec documents are kept local per project instruction, not committed.
SOFTWARE-SPEC.md
*-EDITOR-SPEC.md
PIANO-KEYBOARD-SPEC.md
PATTERN-LIST-SPEC.md
MIXER-COMPARISON.md
mixer-comparison-screenshots/

# Local-only assets carried over from the other PlayerPro directory --
# test files and reference notes, not part of the project itself.
MADK Files/
local-notes/

# Local build/sign/notarize scripts and their output -- never committed,
# per project instruction (the process itself, and any credentials it
# touches, stay off GitHub entirely).
release/
